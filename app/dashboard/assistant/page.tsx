'use client';
import {useState} from 'react';
export default function Page(){const[q,setQ]=useState(''),[a,setA]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');async function ask(e:React.FormEvent){e.preventDefault();if(!q.trim())return;setBusy(true);setError('');try{const r=await fetch('/api/assistant',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({question:q})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Assistant request failed');setA(d.answer||'No answer')}catch(e){setError(e instanceof Error?e.message:'Assistant unavailable')}finally{setBusy(false)}}return <div className="mx-auto max-w-3xl"><h1 className="text-3xl font-bold">Smart Shop Assistant</h1><p className="mt-2 text-slate-500">Ask about sales, profit, receivables, payables or stock.</p><form onSubmit={ask} className="mt-6 flex flex-col gap-3 sm:flex-row sm:items-end"><div className="flex-1"><label className="form-label">Your question</label><input className="field" value={q} onChange={e=>setQ(e.target.value)} placeholder="আজকের বিক্রি কেমন?" aria-label="Your question" required/></div><button disabled={busy} aria-busy={busy} className="btn-primary w-full sm:w-auto">{busy?<span className="spinner"/>:null}{busy?'Thinking…':'Ask'}</button></form>{error&&<div className="mt-4 rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">{error}</div>}{a&&<div className="mt-5 rounded-2xl border bg-white p-5 text-lg">{a}</div>}</div>}
