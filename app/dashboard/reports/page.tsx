'use client';
import {useCallback,useEffect,useState} from 'react';
import {apiRequest} from '@/lib/api-client';
export default function Page(){const[d,setD]=useState<any>();const[error,setError]=useState('');const load=useCallback(async()=>{try{setError('');setD(await apiRequest('/api/reports?days=30'))}catch(e){setError(e instanceof Error?e.message:'Could not load report')}},[]);useEffect(()=>{load()},[load]);const cards=[['Sales',d?.sales],['COGS',d?.cogs],['Gross profit',d?.grossProfit],['Expenses',d?.expenses],['Net profit',d?.netProfit],['Collections',d?.collections]];return <div className="mx-auto max-w-6xl"><h1 className="text-3xl font-bold">রিপোর্ট</h1><p className="mt-2 text-slate-500">Last 30 days business summary.</p>{error&&<div className="mt-5 flex items-center justify-between rounded-2xl border border-red-200 bg-red-50 p-4 text-sm text-red-700"><span>{error}</span><button className="btn-secondary" onClick={load}>Retry</button></div>}{!d&&!error?<div className="mt-6 grid animate-pulse gap-4 sm:grid-cols-2 lg:grid-cols-3">{Array.from({length:6}).map((_,i)=><div key={i} className="h-28 rounded-2xl bg-slate-200"/>)}</div>:d?<div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{cards.map(([k,v])=><div className="rounded-2xl border bg-white p-5" key={k}><p className="text-sm text-slate-500">{k}</p><p className="mt-2 text-2xl font-bold">৳{Number(v||0).toLocaleString()}</p></div>)}</div>:null}<div className="mt-5 rounded-2xl border bg-white p-5">{d?<><span className="text-slate-500">Transactions:</span> {d.transactions??0}</>:<span className="text-slate-400">No report data.</span>}</div></div>}
