import {NextResponse} from "next/server";import {auth} from "@/auth";import {createPurchase} from "@/services/businessService";import {connectDB} from "@/lib/db";import Transaction from "@/models/Transaction";import {Types} from "mongoose";import {z} from "zod";export async function GET(){const s=await auth();if(!s?.user?.id)return NextResponse.json({error:"Unauthorized"},{status:401});await connectDB();return NextResponse.json(await Transaction.find({userId:new Types.ObjectId(s.user.id),type:"STOCK_IN"}).sort({timestamp:-1}).limit(200).populate("partyId","name").populate("productId","name unit").lean())}export async function POST(req:Request){const s=await auth();if(!s?.user?.id)return NextResponse.json({error:"Unauthorized"},{status:401});try{const b=await req.json();const v=z.object({partyId:z.string().nullable().optional(),productId:z.string().min(1),quantity:z.coerce.number().finite().positive(),unitPrice:z.coerce.number().finite().positive(),paidAmount:z.coerce.number().finite().min(0),notes:z.string().max(500).optional()}).parse(b);return NextResponse.json({ok:true,result:await createPurchase(s.user.id,v)},{status:201})}catch(e:any){return NextResponse.json({error:e.message,code:e.code},{status:e.code==="PARTY_REQUIRED"?422:400})}}