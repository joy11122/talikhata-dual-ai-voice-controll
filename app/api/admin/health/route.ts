import {NextResponse} from 'next/server'; import {auth} from '@/auth'; import {connectDB} from '@/lib/db'; import mongoose from 'mongoose';
export async function GET(){const s=await auth();if(!s?.user?.id)return NextResponse.json({error:'Unauthorized'},{status:401,headers:{'Cache-Control':'no-store'}});if(s.user.role!=='ADMIN')return NextResponse.json({error:'Forbidden'},{status:403,headers:{'Cache-Control':'no-store'}});let database='unknown';try{await connectDB();database=mongoose.connection.readyState===1?'connected':'degraded'}catch{database='error'}return NextResponse.json({database,node:process.version,nextRuntime:'nodejs',ai:{openaiConfigured:Boolean(process.env.OPENAI_API_KEY),openrouterConfigured:Boolean(process.env.OPENROUTER_API_KEY)},environment:process.env.VERCEL?'vercel':'self-hosted'},{headers:{'Cache-Control':'no-store'}})}
