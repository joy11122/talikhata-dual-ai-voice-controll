'use client';
import {useEffect,useState} from 'react'; import {ShieldAlert} from 'lucide-react'; import {LoadingSpinner} from '@/components/LoadingSpinner';
export default function Security(){const[d,setD]=useState<any>();const[e,setE]=useState('');useEffect(()=>{fetch('/api/admin/security').then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setD(j)}).catch(x=>setE(x.message||'Unable to load security data'))},[]);if(e)return <p className="rounded-xl bg-red-50 p-4 text-sm text-red-700">{e}</p>;if(!d)return <LoadingSpinner label="Loading security center…"/>;return <div><div className="flex items-start gap-3"><ShieldAlert className="mt-1 text-amber-600"/><div><h1 className="text-[30px] font-semibold tracking-[-.035em]">Security Center</h1><p className="mt-2 text-slate-500">Review access restrictions, failed voice operations, and admin actions.</p></div></div><div className="mt-6 grid gap-4 md:grid-cols-3"><section className="rounded-2xl border border-black/[.07] bg-white p-5"><h2 className="font-semibold">Suspended users</h2><p className="mt-2 text-3xl font-semibold">{d.suspended.length}</p></section><section className="rounded-2xl border border-black/[.07] bg-white p-5"><h2 className="font-semibold">Failed voice operations</h2><p className="mt-2 text-3xl font-semibold">{d.failedVoice.length}</p></section><section className="rounded-2xl border border-black/[.07] bg-white p-5"><h2 className="font-semibold">Admin actions</h2><p className="mt-2 text-3xl font-semibold">{d.recentAdmin.length}</p></section></div><section className="mt-6 rounded-2xl border border-black/[.07] bg-white p-5"><h2 className="font-semibold">Recent failed voice operations</h2><div className="mt-4 space-y-2">{d.failedVoice.slice(0,12).map((x:any)=><div key={String(x._id)} className="rounded-xl bg-red-50 p-3 text-sm"><p className="font-medium">{x.voiceTranscript||'No transcript'}</p><p className="mt-1 text-red-700">{x.errorMessage||'Execution failed'} · {new Date(x.createdAt).toLocaleString('en-BD')}</p></div>)}</div></section></div>}
